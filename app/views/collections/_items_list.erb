<%#
# To change this template, choose Tools | Templates
# and open the template in the editor.
%>

<% if @items.any? %>
            <%=will_paginate @items %>
                <% @items.each do |item| %>
                        <%=render :partial => 'item_row', :locals => {:item => item }%>

                <% end -%>
                <%# end @listings.each -%>
            <%= will_paginate @items %>
        <% else -%>
                No items match your search criteria.  Please try another <%#link_to('search', root_path)%>.
<% end -%>
